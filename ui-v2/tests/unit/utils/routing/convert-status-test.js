import routingConvertStatus from 'consul-ui/utils/routing/convert-status';
import { module, test } from 'qunit';

module('Unit | Utility | routing/convert status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = routingConvertStatus();
  assert.ok(result);
});
