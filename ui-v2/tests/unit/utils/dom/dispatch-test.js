import domDispatch from 'consul-ui/utils/dom/dispatch';
import { module, test } from 'qunit';

module('Unit | Utility | dom/dispatch');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = domDispatch();
  assert.ok(result);
});
